import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors
  static const Color primary = Color(
    0xFF2196F3,
  ); // Blue color for primary actions
  static const Color secondary = Color(
    0xFF03DAC6,
  ); // Teal for secondary elements

  // Background colors
  static const Color background = Colors.white;
  static const Color surface = Colors.white;

  // Text colors
  static const Color textPrimary = Color(
    0xFF212121,
  ); // Dark text for primary content
  static const Color textSecondary = Color(
    0xFF757575,
  ); // Grey for secondary text
  static const Color textHint = Color(0xFFBDBDBD); // Light grey for hints

  // State colors
  static const Color error = Color(0xFFB00020); // Red for error states
  static const Color success = Color(0xFF4CAF50); // Green for success states
  static const Color warning = Color(0xFFFFC107); // Amber for warning states

  // Input field colors
  static const Color inputFillColor = Color(
    0xFFF5F5F5,
  ); // Light grey for input backgrounds

  // Divider and border colors
  static const Color divider = Color(0xFFE0E0E0); // Light grey for dividers
}
